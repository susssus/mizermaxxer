# Magazine bibliography

Magazine coverage is stored in:

- [`../publications.yaml`](../publications.yaml) — master publication list
- [`../issues/`](../issues/) — one YAML file per issue (articles nested inside)

The print bibliography site section reads from these files. Historical exports also land in [`../../exports/`](../../exports/).
